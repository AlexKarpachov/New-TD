/// <summary>
/// This class exists as a placeholder for tower-specific behaviors.
/// Currently, it inherits all functionality from TowerBase without modifications.
/// 
/// If future towers require unique logic (e.g., special attack modes, unique upgrades),
/// this class can be extended with additional methods and overrides.
/// Otherwise, all towers can simply use TowerBase without separate classes.
/// </summary>
public class MageLvl2 : TowerBase
{
    // No additional logic needed for now. Future unique behaviors can be added here.
}
